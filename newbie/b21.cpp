#include<bits/stdc++.h>
using namespace std;
int main(){
    int hour,minutes,second;
    cin>>hour>>minutes>>second;
    int dem=0;
    if((hour>=0&&hour<24) && (minutes>=0&&minutes<60)&&(second>=0&&second<60)){
        cout<<hour<<" "<<minutes<<" "<<second+1;
        if(minutes==59||second==59){
            cout<<
        }
    }
}